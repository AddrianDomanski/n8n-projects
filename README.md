# n8n Workflows Collection

This repository contains a collection of **n8n workflows** for automation tasks. Each workflow is provided as a JSON file and can be imported directly into your n8n instance.
